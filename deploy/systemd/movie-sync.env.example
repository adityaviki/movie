# Copy to /etc/movie-sync.env on the Lightsail box (mode 0640 root:ubuntu)
# Used by movie-imdb-sync.service.

DATABASE_URL=postgres://movie:REPLACE_ME@127.0.0.1:5432/movie_manager
OMDB_API_KEY=950b9f99
OMDB_MAX_CALLS_PER_RUN=800
IMDB_CACHE_DIR=/var/lib/movie-sync/imdb-cache
